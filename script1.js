//A
/*const superheroes = ([   
    { name: "Batman", alter_ego: "Bruce Wayne" },
    { name: "Superman", alter_ego: "Clark Kent" },
    { name: "Spiderman", alter_ego: "Peter Parker" }
]);
const searchedItem = superheroes.find(findSpiderMan);

// Hier komt jouw functie 
function findSpiderMan(Element){
    return Element.name == "Spiderman";
};
console.log(searchedItem);*/
 // Find Spiderman
// result should be: {name: "Spiderman", alter_ego: "Peter Parker"

//B
/*const doubleNumbers = [1,2,3];

const doubleArrayNumbers =  doubleNumbers(findNumbers);

function findNumbers(doubleNumber,index) {
    return Element.doubleNumbers.forEach(element => {
         }
});

consolo.log (doubleArrayValues([1, 2, 3]); 
  // result should be [2, 4, 6]*/

const doubleArrayValues = [1, 2, 3,];
let sum = 0;

 doubleArrayValues.forEach(item => {
     sum += item;
     console.log(sum)
 });  
